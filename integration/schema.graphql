interface HasName {
  name: String!
}

type Author implements HasName {
  name: String!
}

type Book implements HasName {
  name: String!
}

type NoImportNeeded implements HasName {
  name: String!
}
